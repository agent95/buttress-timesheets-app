# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the main branch
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2

      # Runs a single command using the runners shell
      - name: Run a one-line script
        run: echo Hello, world!

      # Runs a set of commands using the runners shell
      - name: Run a multi-line script
        run: |
          echo Add other actions to build,
          echo test, and deploy your project.

      - name: Simple FTP deploy
        # You may pin to the exact commit or the version.
        # uses: kevinpainchaud/simple-ftp-deploy-action@2c6e7dfe809abd4f8f687a78b5d79efaf5c439ad
        uses: kevinpainchaud/simple-ftp-deploy-action@v1.2.1
        with:
          # FTP host
          ftp_host: 167.99.10.209
          # FTP username
          ftp_username: root
          # FTP password
          ftp_password: Buttre55Timesh33tsApp
          # The local folder to copy
          local_source_dir: /dist/timesheets
          # The remote folder
          dist_target_dir: /var/www/html
          # Delete files not present in the local folder on the remote folder
          delete: true # optional, default is false 
          # Download only newer files
          only_newer: true # optional, default is false
          # Ignore time when deciding whether to download
          ignore_time: false # optional, default is false
          # Ignore file(s) and/or directorie(s)
          exclude: false # optional, default is 
          # Disable SSL certificate verification
          disable_ssl_certificate_verification: true # optional, default is true
          # Optional raw string of flags for lftp eg. --no-empty-dirs
          other_flags: --no-empty-dirs # optional, default is 
